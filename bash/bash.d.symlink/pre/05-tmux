case $- in
    *i*) ;;
      *) return;;
esac

#if cmd_exists tmux; then
  ## if not in a tmux session, start or attach to "login"
  #if [ -z $TMUX ]; then
    #tmux new-session -A -s login
  #fi
#fi
