#!/usr/bin/env bash

CURRENT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

source ${CURRENT_DIR}/../helpers.sh

if is_macos
then
  brew install vim
elif is_termux
then
  pkg install -y vim
fi
